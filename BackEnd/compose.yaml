services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: exportpro_db
    ports:
      - "3306:3306"
    volumes:
      - exportpro_mysql_data:/var/lib/mysql

volumes:
  exportpro_mysql_data: